# =================================================================
# 🏨 ROOM SERVICE — PLANTILLA DE VARIABLES DE ENTORNO (.env.example)
# =================================================================
# USO (en máquinas nuevas después de git clone):
#   1) Copiar este archivo a uno llamado ".env" en la MISMA carpeta:
#        cp .env.example .env          # Linux/Mac
#        copy .env.example .env        # Windows PowerShell
#   2) Editar .env y reemplazar placeholders (DB_PASSWORD, JWT_SECRET, etc.).
#
#   ⚠️  NUNCA subas el .env REAL a Git (lo cubre services/*/.env en .gitignore).
#      Esta plantilla (.env.example) SÍ se sube, para que el equipo tenga las
#      variables documentadas.

# ================================================
# CONFIGURACIÓN DEL SERVIDOR
# ================================================
# Puerto en el que escuchará la API REST de habitaciones
PORT=4001

# Entorno de ejecución: development | production | test
NODE_ENV=development

# URL pública del frontend (para configuración de CORS / whitelist)
FRONTEND_URL=http://localhost:3000

# URL pública de ESTE microservicio (utilizada al suscribirse al Broker Pub/Sub
# para decirle al broker "POSTear los eventos AQUÍ").
ROOM_SERVICE_URL=http://localhost:4001

# ================================================
# BASE DE DATOS (MySQL — Sequelize)
# ================================================
DB_NAME=sistema-reservas
DB_USER=root
DB_PASSWORD=tu_contraseña_mysql
DB_HOST=localhost
DB_PORT=3306
DB_DIALECT=mysql
DB_SSL=false

# ================================================
# POOL DE CONEXIONES DE BD
# ================================================
DB_POOL_MAX=5
DB_POOL_MIN=0
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000

# ================================================
# AUTENTICACIÓN JWT
# ================================================
# Generar clave segura en producción:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=secret_key_change_in_production
JWT_EXPIRES_IN=24h

# ================================================
# HASHING DE CONTRASEÑAS (BCRYPT)
# ================================================
BCRYPT_SALT_ROUNDS=10

# ================================================
# COMUNICACIÓN ENTRE MICROSERVICIOS
# ================================================
# URL del MESSAGE BROKER central (Puerto 4003 — Patrón Pub/Sub GoF).
# Room se SUSCRIBE automáticamente al tópico RESERVA_CREADA al iniciar
# (ver index.js sección "AUTO-SUSCRIPCIÓN Pub/Sub").
BROKER_URL=http://localhost:4003
